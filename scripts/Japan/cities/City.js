export const City = (city) => {
    return `
    <img class="countyFactsImage" src="">
        
            
                    <li class="cities countryArticles">${city.name}</li>
                    
                `
}
