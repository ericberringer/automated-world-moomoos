export const Celebrities = (celebrity) => {
    return `
        <p class="city__name">${celebrity.name}</p>
    <img src="${celebrity.image}" alt="cityImage" "class=landmarks" width="250px" height="200px">
    `
}